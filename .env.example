# Slack webhook for the #marketing-agents channel
# (or whichever channel you wire the webhook to — fully configurable)
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXX/YYY/ZZZ

# Optional: explicit repo root (defaults to script's parent)
# REPO_ROOT=/absolute/path/to/claude-cmo-agent
